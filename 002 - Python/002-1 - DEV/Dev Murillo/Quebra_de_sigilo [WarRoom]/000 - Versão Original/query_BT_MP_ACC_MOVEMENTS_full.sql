-- ============================================================================
-- CONSULTA COMPLETA: WHOWNER.BT_MP_ACC_MOVEMENTS
-- ============================================================================
-- Todas as colunas, uma linha de cabeçalho (nomes das colunas) + dados.
-- Execute no BigQuery e exporte o resultado para CSV/TXT se precisar do log.
-- Para teste com poucas linhas, descomente a linha LIMIT abaixo.
-- ============================================================================

SELECT *
FROM `meli-bi-data.WHOWNER.BT_MP_ACC_MOVEMENTS`
-- LIMIT 1000   -- descomente para teste com 1000 linhas
